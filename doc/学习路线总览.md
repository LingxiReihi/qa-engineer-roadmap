# 软件测试学习路径（重构版）

> **版本**：v2.0（重构自原始 180 天计划 + `_docs/` 教学内容）
> **重构日期**：2026-09-18
> **当前进度**：阶段 01 · Day 4 / 21（20%）
> **教学方式**：陪练式 —— 每日按「学习 → 实战 → 提交 → Code Review → 修正 → 验收」推进
> **教学数据库**：teach.db（SQLite，45 知识点，2 类别，6 会话，21 术语）

---

## 一、当前状态

| 维度 | 值 |
|------|------|
| 当前阶段 | **阶段 01：Python 语言基础（测试视角）** |
| 当前天数 | **Day 4 / 21** |
| 日历进度 | 第 1~3 周（实际：9/15~9/18，约 3.5 天完成 4 天课程，节奏快于计划） |
| 已掌握知识点 | 4 个（Day0 工程环境、Day1 变量/容器、Day2 控制流/推导式、Day3 函数/纯函数） |
| 进行中知识点 | 1 个（Day4 模块与包——greeting 包已完成，calc 包待实现） |
| 累计教学会话 | 6 次 |
| 累计术语积累 | 21 条 |
| 作品仓库 | `qa-engineer-roadmap`（GitHub，已建立，已 push） |

---

## 二、总体路线图（8 阶段 · 180 天）

```text
阶段 01 · Python 语言基础（测试视角）      3 周  ← 【当前】Day 4/21
    ↓
阶段 02 · 测试理论与用例设计               2 周
    ↓
阶段 03 · pytest 核心与进阶               4 周
    ↓
阶段 04 · 接口自动化测试                   4 周
    ↓
阶段 05 · CI/CD 与工程化                   3 周
    ↓
阶段 06 · Web 与移动端自动化               3 周
    ↓
阶段 07 · 性能测试与监控                   2 周
    ↓
阶段 08 · 测试开发进阶 + 项目实战          5 周
    ↓
最终作品：QA Automation Platform
```

> 阶段 05/06/07 都只依赖 04，顺序可微调。
> 未通过上一阶段验收前，不进入下一阶段。

---

## 三、已完成内容（Day 0 ~ Day 4）

### Day 0 · 工程环境准备 ✅（9/16）

| 项目 | 状态 |
|------|------|
| 作品集仓库 `qa-engineer-roadmap` | ✅ 已建立、已 push GitHub |
| 虚拟环境 `.venv` | ✅ 已创建并激活 |
| `.gitignore` | ✅ 已写好（含踩坑记录） |
| 目录结构 01~10 + `.github/workflows` | ✅ 全部含 `.gitkeep` |
| 旧仓库处理 | ✅ 移入 `old/` |

**教学数据库**：topic #51「Day0 作品集仓库初始化」→ mastered（1.0）

---

### Day 1 · 变量 / 数据类型 / 容器（测试视角）✅（9/16）

**训练内容**：
- 4 种基本类型（int/float/str/bool）与 4 种容器（list/dict/set/tuple）
- 可变 vs 不可变、别名陷阱
- 列表/字典/集合推导式
- 类型注解 + assert 断言自检

**实战任务 T1-1**：`containers.py`（三个函数）+ `t1_1_comprehensions.py`（三道推导式）

| 函数 | 首提问题 | 修正 |
|------|----------|------|
| `count_words` | 非字符串处理 ✓ | 新增 `" "` 边界用例 |
| `unique_sorted` | ❌ set 去重后未排序（`[10,1,2]` → `[1,10,2]`） | 补 `res.sort()` |
| `merge_dicts` | ❌ `a.update(b)` 污染调用方原字典 | 改 `{**a, **b}` |

**Review**：首提 **C** → 修正后 **B+（通过）**

**最值钱的一课**：测试全绿 ≠ 需求满足——断言预期必须来自需求，而不是"代码碰巧输出什么"。

**教学数据库**：topic #52 → mastered（0.85）

---

### Day 2 · 控制流（if/for/while）+ 列表推导式 ✅（9/17）

**训练内容**：
- if/elif/else 分支 + truthy/falsy
- for/while 遍历 + break/continue + while 必须有出口
- 推导式 vs 显式循环
- 不可信输入校验（`str.isdigit()`）
- 输入与逻辑分离（可测试性第一步）

**实战任务 T1-2**：`t1_2_guess.py`（猜数字 `guess_number(target, max_attempts=5)`）

**验收**：猜中 True、超限 False、非法输入不崩溃

**Review**：**A-（通过）**

**教学数据库**：topic #53 → mastered（0.88）

---

### Day 3 · 函数（测试视角）✅（9/17）

**训练内容**：
- 函数签名：参数、返回值、默认参数
- `*args` / `**kwargs`
- **可变默认参数陷阱**（`def f(x=[])`）—— 今天最值钱的一课
- **纯函数**（同输入同输出、无副作用）—— 测试开发最爱的函数形态

**实战任务 T1-3**：`t1_3_discount.py`（`discount(price, rate=1.0, coupon=0.0)`）

**验收**：4 条 assert（正常/折扣/券超价钳位/券超价钳位）

**Review**：首提 **B+** → 修正后 **A-**

**教学数据库**：topic（待添加）→ 应记录为 in_progress

---

### Day 4 · 模块与包（测试视角）◐（9/18）

**训练内容**：
- 模块 vs 包（目录 + `__init__.py`）
- import 三种形态
- `if __name__ == "__main__":` 守卫
- 包结构 = 被测系统骨架

**实战任务 T1-4**：`calc/` 包（四则运算 + 工具函数 + 包外测试脚本）

**当前状态**：
- ✅ `greeting/` 练习包已完成（hello.py + __init__.py）
- ◐ `calc/` 包结构已创建但文件为空（arithmetic.py, utils.py, test_calc.py 均为 0 字节）
- 浮点断言坑、multiply 导出实验待完成

**下一步**：完成 `calc/arithmetic.py`（3 个纯函数）+ `calc/utils.py`（clamp, round2）+ `test_calc.py`（包外测试脚本，含浮点断言处理）

---

## 四、阶段 01：Python 语言基础（进行中 · Day 4/21）

### 4.1 已完成（Day 0~4）

| Day | 主题 | 训练编号 | Review | 数据库状态 |
|-----|------|----------|--------|------------|
| 0 | 工程环境准备 | — | ✅ | mastered (1.0) |
| 1 | 变量/类型/容器/推导式 | T1-1 | B+ ✅ | mastered (0.85) |
| 2 | 控制流/推导式/猜数字 | T1-2 | A- ✅ | mastered (0.88) |
| 3 | 函数/默认参数/纯函数 | T1-3 | A- ✅ | in_progress (待记录) |
| 4 | 模块与包 | T1-4 | ◐ 进行中 | in_progress (待记录) |

### 4.2 待完成（Day 5~21）

| Day | 主题 | 训练编号 | 核心知识点 | 依赖 |
|-----|------|----------|------------|------|
| 5 | 异常处理与自定义异常 | T1-5 | try/except/else/finally、自定义异常、异常链 | Day 3 |
| 6 | 文件 IO 与 JSON | T1-6 | 读写、with、JSON 序列化/反序列化、CSV | Day 1 |
| 7 | 字符串与正则基础 | T1-7 | 格式化、split/join、re 模块基础 | Day 2 |
| 8~9 | 面向对象基础 | T1-8 | class/实例/类方法/静态方法、self | Day 3 |
| 10 | 继承与多态 | T1-9 | issubclass、方法重写、duck typing | Day 8~9 |
| 11~12 | 装饰器 | T1-10 | 函数作为参数、闭包、自定义装饰器 | Day 3 |
| 13 | 生成器与迭代器 | T1-11 | yield、next、itertools 基础 | Day 2 |
| 14 | 上下文管理器 | T1-12 | with、__enter__/__exit__ | Day 6 |
| 15~16 | 网络基础与 HTTP | T1-13 | requests 基础、HTTP 状态码、JSON API | Day 6 |
| 17 | 数据处理与集合 | T1-14 | collections.Counter、defaultdict、数据清洗 | Day 1 |
| 18~19 | 小项目：命令行工具 | T1-15 | 整合前 17 天知识，CLI 参数、文件输出 | Day 5~14 |
| 20~21 | pytest 入门 + 覆盖率 | T1-16 | assert、测试函数、fixture 基础、coverage | Day 1~3 |

### 4.3 阶段 01 验收标志

> **用 pytest 给一个 50 行的模块写单元测试，覆盖率 > 80%。**

---

## 五、后续阶段概览

### 阶段 02 · 测试理论与用例设计（2 周 · 预计 Day 22~35）

**核心能力**：拿到需求文档能写出结构化用例

| 知识点 | 训练 | 验收 |
|--------|------|------|
| 黑盒测试方法（等价类/边界值/场景法/错误推测） | 登录功能 30+ 用例 | 用例覆盖正常/异常/边界/安全 |
| 测试文档规范（ID/标题/步骤/预期/优先级） | 用例模板标准化 | 用例可复用 |
| 测试计划与评审 | 小项目测试计划 | 计划经评审通过 |
| Linux 日志分析（grep/awk） | ERROR/WARNING 提取 | 能定位问题行 |

### 阶段 03 · pytest 核心与进阶（4 周 · 预计 Day 36~63）

**核心能力**：掌握 fixture/parametrize/conftest/hook/Mock/标记/插件/报告

| 知识点 | 训练 | 验收 |
|--------|------|------|
| fixture（作用域/yield/参数化） | 数据驱动测试 | fixture + parametrize 组合 |
| conftest.py 与测试组织 | 多模块测试套件 | 跨模块复用 fixture |
| Mock 与外部依赖替换 | Mock 网络/文件/数据库 | 可脱离外部环境测试 |
| 测试覆盖率与门禁 | coverage > 80% | 覆盖率不达标阻断 CI |
| Allure 报告 | 测试报告输出 | 输出含图表的报告 |

### 阶段 04 · 接口自动化测试（4 周 · 预计 Day 64~91）

**核心能力**：用 requests + pytest 写接口自动化

| 知识点 | 训练 | 验收 |
|--------|------|------|
| requests 基础与断言 | 公开 API 测试 | 20+ 用例 |
| 鉴权与 Token（JWT/OAuth2） | 登录 + 多接口套件 | Session + Token 刷新 |
| 接口 Mock（responses/requests-mock） | Mock 外部依赖 | 无需真实外部服务 |
| OpenAPI 契约测试 | 接口规范验证 | 自动验证响应 schema |
| 数据驱动与工程模式 | 参数化 + 数据工厂 | 数据与逻辑分离 |
| SQL 测试数据准备 | 准备/验证/清理数据 | 测试数据可复用 |

### 阶段 05 · CI/CD 与工程化（3 周 · 预计 Day 92~104）

| 知识点 | 训练 | 验收 |
|--------|------|------|
| Git 分支策略与 PR | 分支保护 | PR 合并流程 |
| GitHub Actions CI | 自动跑测试 | 推 PR 后自动触发 |
| Docker 容器化 | 测试环境容器化 | Dockerfile + compose |
| Allure 报告集成 | 报告归档 | CI 中生成并上传报告 |

### 阶段 06 · Web 与移动端自动化（3 周 · 预计 Day 105~117）

| 知识点 | 训练 | 验收 |
|--------|------|------|
| Playwright Web 自动化 | 登录页测试 | PO 模式 |
| Page Object Model | 多步骤流程 | 登录→搜索→下单→登出 |
| Appium 移动端 | Android 定位与操作 | 基础移动自动化 |

### 阶段 07 · 性能测试与监控（2 周 · 预计 Day 118~126）

| 知识点 | 训练 | 验收 |
|--------|------|------|
| Locust 压测基础 | 单接口压测 | 输出含图表的报告 |
| 性能指标解读（P95/P99/TPS/错误率） | 指标分析 | 能解释关键指标 |
| 瓶颈定位 | 多场景压测 | 识别 CPU/IO/网络瓶颈 |

### 阶段 08 · 测试开发进阶 + 项目实战（5 周 · 预计 Day 127~180）

**核心作品：QA Automation Platform**

```text
qa-automation-platform/
├── api/              # 接口测试客户端 + 端点
├── web/              # 页面对象 + 组件
├── tests/            # api/ web/ database/ 分层测试
├── data/             # 测试数据
├── config/           # 配置管理
├── utils/            # 工具函数
├── logs/             # 日志
├── reports/          # 报告
├── conftest.py       # pytest 配置
├── pytest.ini        # pytest 配置
├── requirements.txt  # 依赖
├── Dockerfile        # 容器化
├── docker-compose.yml # 编排
└── README.md         # 文档
```

**最终 CI 流水线**：
```
git push → GitHub Actions → 启动测试环境 → 运行 Pytest
→ API 测试 → Web 测试 → 数据库验证 → 生成报告 → 测试结果
```

---

## 六、陪练式教学协议

### 6.1 每日固定 7 部分流程

每天说一句「**开始 Day X**」，AI 按 7 部分输出：

| 部分 | 内容 | 原则 |
|------|------|------|
| ① 今日目标 | 3~5 条，明确「今天结束应能做到什么」 | 可验证 |
| ② 理论课 | 只讲当天必须掌握的知识 | 每个概念：定义 + 示例 + 坑 |
| ③ 示例代码 | 少量、能跑、标注关键点 | ≤ 30 行，含反例 |
| ④ 今日练习 | 10 分钟能完成的思考/编码小题 | 不查资料，每题 ≤ 5 行 |
| ⑤ 今日实战任务 | 模拟真实测试开发工作 | 含正常+异常+边界 |
| ⑥ 提交作业 | 代码 + 运行结果 + 测试场景 + 问题 | 按模板提交 |
| ⑦ Code Review | 7 维度评分 + 修改建议 | 不给答案 |

### 6.2 Code Review 7 维度

| 维度 | 检查点 |
|------|--------|
| 正确性 | 功能是否符合需求；边界是否覆盖 |
| 代码结构 | 函数是否单一职责；模块是否分层 |
| 命名 | 变量/函数/模块名是否表意 |
| 异常处理 | 是否捕获该捕获的异常；是否吞掉不该吞的 |
| 可读性 | 是否能被第三人读懂；注释是否恰当 |
| 测试思维 | 是否考虑了正常/异常/边界/并发 |
| 工程习惯 | 虚拟环境、.gitignore、类型注解、文档字符串 |

### 6.3 评分系统 S/A/B/C/D

| 综合 | 判定 | 后续 |
|------|------|------|
| S / A | ✅ 通过 | 进入下一天 |
| B | ⚠️ 修改后通过 | 重交修改版 |
| C / D | ❌ 需要重做 | 重学 + 重交 |

**铁律**：代码运行成功 ≠ 通过。`print("Hello")` 能跑，但不代表写得好。

### 6.4 不代写原则

**可以做的**：讲思路、指出异常、给测试方向、点评问题、解释概念、提供反例
**不做的**：直接给答案、贴练习答案、帮你写代码、跳过 Review、降低标准

---

## 七、教学数据库集成

### 7.1 数据库结构

| 表 | 用途 | 当前数量 |
|----|------|----------|
| categories | 学习类别 | 2 |
| topics | 知识点 | 45 |
| progress | 学习进度（mastery/status） | 45 |
| sessions | 教学会话 | 6 |
| terms | 术语积累 | 21 |
| interactions | 关键交互记录 | 6 |
| mistakes | 结构化错误 | 0（待记录） |
| practice_records | 练习证据 | 0（待记录） |
| reviews | 间隔复习 | 0 |
| projects | 项目追踪 | 0 |

### 7.2 进度映射规则

| Review 评分 | mastery | status |
|-------------|---------|--------|
| S | 0.9 ~ 1.0 | mastered |
| A | 0.7 ~ 0.9 | mastered |
| B | 0.5 ~ 0.7 | in_progress |
| C | 0.3 ~ 0.5 | in_progress |
| D | 0.0 ~ 0.3 | not_started |

### 7.3 掌握度模型

- **证据驱动**：mastery 由 `mastery show / apply` 计算，不手工拍数值
- **晋级门槛**：mastery≥0.85 + 证据≥5 + 阶梯段≥3 + 独立成功 + 变式迁移 + 最近无错
- **降级机制**：错误类型 × 重复次数 × 时间衰减（14 天半衰期）
- **confidence 与 mastery 独立**：confidence 反映证据量，mastery 反映当前水平

### 7.4 数据库状态

| 项目 | 状态 |
|------|------|
| schema_version | 3 |
| sync_pending | 0（待同步：本次更新需手动复制到 OneDrive） |
| 数据库文件 | `teach.db`（151KB，2 类别，45 知识点） |

---

## 八、下一步行动

### 立即（Day 4 完成）

1. 完成 `calc/arithmetic.py`（add/subtract/multiply，含浮点断言处理）
2. 完成 `calc/utils.py`（clamp/round2，含浮点坑注释）
3. 完成 `calc/__init__.py`（导出策略，故意不导出 multiply）
4. 完成 `test_calc.py`（包外测试脚本，≥3 条/函数，含 multiply 导出实验）
5. 提交作业，等待 Code Review

### 下周（Day 5~7）

- Day 5：异常处理与自定义异常
- Day 6：文件 IO 与 JSON
- Day 7：字符串与正则基础

### 本阶段（Day 8~21）

- Day 8~14：面向对象 + 装饰器 + 生成器 + 上下文管理器
- Day 15~17：网络基础 + 数据处理
- Day 18~21：小项目 + pytest 入门

> 路径是死的，人是活的。**每完成一个阶段，回头调整下一阶段的时间**。
